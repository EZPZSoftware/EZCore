bin_PROGRAMS = date chmod link uname
date_SOURCES = src/date.c
chmod_SOURCES = src/chmod.c
link_SOURCES = src/link.c
uname_SOURCES = src/uname.c

clean:
	rm -rf date chmod link uname

distclean:

	rm -rf date chmod link uname *.o Makefile
